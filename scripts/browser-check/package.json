{
  "name": "haimunia-browser-check",
  "private": true,
  "version": "1.0.0",
  "description": "On-demand real-Chromium smoke tests for service worker, fonts, offline behavior, and DOM interactions.",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "check:boot": "node boot-smoke.mjs",
    "check:ladder": "node ladder.mjs",
    "check:update": "node update-flow.mjs",
    "check:all": "node run-all.mjs"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "axe-core": "^4.13.0"
  }
}
